name: Build Vault Admin (Release)

on:
  workflow_dispatch: # बटन दबाने पर चलेगा

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # Gradle 8.2 (ताकि क्रैश न हो)
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.2'

      # 🔴 MAIN COMMAND: 'assembleRelease' का मतलब है Release APK
      - name: Build Release APK
        run: gradle assembleRelease --stacktrace --no-daemon

      # APK अपलोड (नाम में 'release' लिखा आएगा)
      - name: Upload Receiver APK
        uses: actions/upload-artifact@v4
        with:
          name: vault-admin-release-unsigned
          path: app/build/outputs/apk/release/*.apk
